<%# (16) %>

<div class="box">
    <article class="media">
        <div class="media-content">
            <div class="content">
                <p><span><%= comment.name %> </span> said...</p>
                <p><%= comment.comment %></p><%# comment model.comment data type%>
            </div>
        </div>

        <%= link_to "Delete Comment", [comment.post, comment], method: :delete, class: "button is-danger", data: { confirm: "Are you sure you wanna delete this?" } %>
    </article>
</div>