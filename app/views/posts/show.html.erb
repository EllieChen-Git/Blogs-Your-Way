<%= content_for :page_title, @post.title %>

<section class="session">
    <div class="container">
        <div class="content">
            <%= @post.content %>
        </div>
    </div>
</section>